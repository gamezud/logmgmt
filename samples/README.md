# samples

Sample log files and sender scripts (`send_syslog.sh`, `post_logs.py`, etc.)
— not added yet (out of scope for this session, which covers project
scaffolding and the storage layer only). The assignment brief (§4) has short
sample logs for each source that will seed this folder.
